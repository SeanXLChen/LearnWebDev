"use strict";
let myName;
let meaningOfLife;
let isLoading;
let flexible; // can be any type
let union; // can be either string or number
let isActive; // can be multiple types
let re = /\w+/g; // regular expression type
myName = 'Dave';
myName = 'Chris'; // can be reassigned
meaningOfLife = 42;
isLoading = true;
const sum = (a, b) => {
    return a + b;
};
const multiply = (a, b) => {
    return a * b;
};
const divide = (a, b) => {
    return a / b;
};
