export default function NewUserLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>
            <h2>Login & Register Layout:</h2>
            {children}
        </>
    )
}